import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.serenebinturongslide',
  appName: 'serene-binturong-slide',
  webDir: 'dist'
};

export default config;
